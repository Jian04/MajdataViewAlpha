<Window x:Class="MajdataEdit.OrganizeOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="zh-CN"
        lex:ResxLocalizationProvider.DefaultAssembly="MajdataEdit"
        lex:ResxLocalizationProvider.DefaultDictionary="Langs"
        Title="{lex:Loc AutoOrganize}"
        Width="360" Height="145"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackground}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{lex:Loc AddMeasureCommentsQuestion}"
                   Foreground="{DynamicResource ButtonForeground}"
                   VerticalAlignment="Center" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{lex:Loc AddComments}" Width="92" Height="30" Margin="0,0,8,0"
                    IsDefault="True" Click="AddComments_Click" />
            <Button Content="{lex:Loc SkipComments}" Width="92" Height="30"
                    Click="SkipComments_Click" />
        </StackPanel>
    </Grid>
</Window>
