<Window x:Class="MajdataEdit.NoteDensityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="zh-CN"
        lex:ResxLocalizationProvider.DefaultAssembly="MajdataEdit"
        lex:ResxLocalizationProvider.DefaultDictionary="Langs"
        Title="{lex:Loc NoteDensityTitle}" Height="340" Width="900"
        Background="{DynamicResource WindowBackground}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" BorderBrush="{DynamicResource ButtonsBackground}"
                BorderThickness="1" CornerRadius="4" ClipToBounds="True">
            <Image x:Name="DensityImage" Stretch="Fill" SnapsToDevicePixels="True" />
        </Border>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="2,7,2,0">
            <Rectangle Width="12" Height="12" Fill="#FF5A8C" VerticalAlignment="Center" />
            <TextBlock Text="{lex:Loc SlideHeadLegend}" Foreground="{DynamicResource ButtonForeground}"
                       Margin="5,0,14,0" VerticalAlignment="Center" />
            <Rectangle Width="12" Height="12" Fill="#37B6FF" VerticalAlignment="Center" />
            <TextBlock Text="{lex:Loc SlideBodyLegend}" Foreground="{DynamicResource ButtonForeground}"
                       Margin="5,0,14,0" VerticalAlignment="Center" />
            <Rectangle Width="12" Height="12" Fill="#FFD23C" VerticalAlignment="Center" />
            <TextBlock Text="Touch" Foreground="{DynamicResource ButtonForeground}"
                       Margin="5,0,14,0" VerticalAlignment="Center" />
            <Rectangle Width="18" Height="2" Fill="#63D2FF" VerticalAlignment="Center" />
            <TextBlock Text="{lex:Loc AudioIntensityLegend}" Foreground="{DynamicResource ButtonForeground}"
                       Margin="5,0,14,0" VerticalAlignment="Center" />
            <TextBlock x:Name="SummaryText" Foreground="{DynamicResource HelperForeground}"
                       Margin="8,0,0,0" VerticalAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>
