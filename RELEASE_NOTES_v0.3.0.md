# MajdataViewAlpha v0.3.0

## 主要新增

- 新增新版 Master / Re:Master 歌曲信息卡片，可在编辑器设置中切换原版或新版。
- Master 与 Re:Master 使用独立模板和独立缓存，避免互相覆盖。
- 支持皮肤目录切换：`Skin/dx`、`Skin/sd`，也可以添加自定义皮肤目录。
- 编辑器字体设置改为真实系统字体族，并增加多个可读字体选项。
- 新增 / 完善 Alpha 语法帮助、格式刷、配置库检索、音频转换与媒体剪辑工具。
- 导出视频支持 30 FPS 和 60 FPS。

## 语法与显示

- 改进 `COLOR` 染色逻辑，保留音符原本明暗、纹理和层次。
- 新增或完善 `SV`、`SIZE`、`ALPHA`、`TEXT`、显示控制、视频特效、背景分段、块注释、地雷音符等 Alpha 扩展语法。
- Re:Master 等级数字使用更深的紫色渐变，14.6 及以上自动显示为 `14+`。
- 波形图时间轴支持录制准备区、歌曲信息展示区和 All Perfect 区域预览。

## 修复与优化

- 歌曲信息卡缓存只在标题、曲师、谱师、BPM、封面或对应难度等级变化时刷新。
- 修改 Master 等级不会删除 Re:Master 缓存，修改 Re:Master 等级也不会删除 Master 缓存。
- 缓存失效时只删除签名文件，保留 PNG，避免 View 回退到实时拼 UI。
- All Perfect 出现时不再强制清空最后一批判定特效。
- 多处播放、暂停、时间轴拖拽、预览和导出流程进行了稳定性修正。

## 已知问题

- `RP` / `RQ` 在终点与起点相差 `0` 或 `1` 时，旋转特效半径仍可能有误差。
- 高分辨率录制叠加多个高强度全屏特效时，性能压力较大。
- 原版限制仍存在：不支持动态比特率 mp3；内置录屏要求 View 分辨率为偶数。
